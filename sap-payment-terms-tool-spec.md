# SAP支払条件（OBB8）提案ツール - MVP仕様メモ（Cursor引き継ぎ用）

> 目的：日本の商習慣で頻出する「締め／支払」パターンをユーザーが選択すると、SAPの **OBB8（支払条件 / Terms of Payment）** に投入する値（T052相当の行セット）を自動提案し、あわせて **注意点（前提・事故りやすい条件）** と **テストケース** を出力できるようにする。

---

## 1. 背景と狙い

- 支払条件は「要件→パラメータ→計算結果」が定型で、テンプレ化に向く。
- ただし、**基準日（Baseline date）運用**や「固定日（10日/25日）払い」など、実装と運用の組み合わせ次第でズレが起きやすい。
- ツールの価値は「設定値の丸暗記」ではなく、
  - **前提条件の明示**
  - **例外・注意の自動提示**
  - **テスト観点の標準化**
  に置く（公開しても価値が下がりにくい）。

---

## 2. MVPスコープ

### 2.1 入力（ユーザー選択）

1) **支払パターン（テンプレ）**
- 例：月末締 翌月末払い / 20日締 翌月末払い / 月末締 翌々月末払い …

2) **基準日運用（Baseline Dateの前提）**
- A：請求書日付（Invoice date）
- B：転記日付（Posting date）
- C：締日基準（締日に寄せて転記・またはZFBDTを締日に寄せる運用）

3) 割引（スキント）
- なし / 1段 / 2段（※MVPでは「なし」or「1段」まででもOK）

4) 休日補正
- MVPは「注意喚起のみ」推奨（営業日補正は別設計になりがち）

### 2.2 出力

- **OBB8投入用の行セット**（Day limitごとの複数行）
- 推奨コード案（命名規則）
- ユーザー向け説明文（例：「20日締 翌月末払」）
- テストケース（最低5本）
- 注意事項（基準日運用と事故パターン）

---

## 3. 日本で頻出するパターン候補（初期ラインナップ案 8〜12件）

### 3.1 月末系
- 月末締 翌月末払い
- 月末締 翌々月末払い
- 月末締 翌月10日払い（※基準日運用に注意）
- 月末締 翌月25日払い（※基準日運用に注意）

### 3.2 20日締系
- 20日締 翌月末払い（2行テンプレで表現）
- 20日締 翌々月末払い（2行テンプレで表現）
- 20日締 翌月20日払い（2行 or 固定日ロジック）

### 3.3 15日締系（案件次第）
- 15日締 翌月末払い

> NOTE：MVPでは「月末払い」「翌月末」「翌々月末」「20日締翌月末」だけでも十分効果が出る。

---

## 4. OBB8（T052）提案の基本データモデル

> ※ここでは“概念フィールド名”で記載。実装はsnake_caseでOK。

### 4.1 行（line）

- `day_limit` : 日限（1〜31、末日は31扱い）
- `fixed_day` : 固定日（10/20/25/31=月末 等）
- `add_months` : 追加月（1=翌月、2=翌々月…）
- `add_days` : 追加日（ネット○日などで使用）

### 4.2 支払条件テンプレ（pattern）

- `pattern_id`
- `display_name`
- `description`
- `recommended_baseline_mode` : A/B/C
- `obb8_lines` : 行の配列
- `warnings` : 文字列配列（基準日運用による注意等）
- `test_cases` : 生成ルール or サンプル固定（MVPは固定でもOK）

---

## 5. テンプレ定義例（MVPで最低欲しい3つ）

### 5.1 月末締 翌月末払い

- `obb8_lines`
  - L1：`day_limit=31, fixed_day=31, add_months=1, add_days=0`
- `recommended_baseline_mode`：A/B/C いずれでも比較的安定
- テスト例
  - 1/10 → 2/末
  - 1/31 → 2/末

### 5.2 月末締 翌々月末払い

- `obb8_lines`
  - L1：`day_limit=31, fixed_day=31, add_months=2, add_days=0`

### 5.3 20日締 翌月末払い（頻出・2行）

- 意味：当月1〜20 → 翌月末、当月21〜末日 → 翌々月末
- `obb8_lines`
  - L1：`day_limit=20, fixed_day=31, add_months=1, add_days=0`
  - L2：`day_limit=31, fixed_day=31, add_months=2, add_days=0`
- テスト例
  - 1/10 → 2/末
  - 1/25 → 3/末

---

## 6. 事故りやすい代表パターン（ツールの価値ポイント）

### 6.1 月末締「翌月10日/25日払い」

- 固定日（10/25）のロジックは、**基準日の日付が固定日を超える**と月が繰り上がり、
  - 例：基準日=1/26 の場合、固定日25だと **2/25ではなく3/25** 側に寄りやすい
- 対策：
  - ツールは `recommended_baseline_mode=C（締日基準）` を推奨
  - A/B（請求書日付/転記日付）選択時は **警告** を必ず表示

### 6.2 休日補正

- OBB8単体では「営業日補正」の扱いが設計次第。
- MVPでは
  - 「休日補正が必要なら別途方針（支払プログラム/銀行連携/支払実行日調整）を検討」
  - と注意喚起を標準出力。

---

## 7. UI/UX（MVP画面案）

### 7.1 画面構成

1) Pattern選択（検索・タグ付き）
- タグ例：`締日=20日` `支払=月末` `サイト=30/60` `固定日あり`

2) Baseline mode選択
- A/B/C ラジオ

3) 割引（スキント）
- なし/あり（段階数）

4) 出力
- OBB8 lines（表形式）
- コード案
- テストケース
- 注意事項

### 7.2 出力の見せ方（例）

- **OBB8投入値（行セット）**
  - `day_limit | fixed_day | add_months | add_days`
- **説明文**：例「20日締 翌月末払（21日以降は翌々月末）」
- **警告**：例「請求書日付基準だと固定日払いで翌々月に飛ぶ可能性」
- **テストケース**：入力日→期待支払日

---

## 8. 実装方針（最短で動かす）

### 8.1 方式：テンプレカタログ（JSON/YAML）

- ルールエンジンをいきなり作らず、まずは
  - `patterns.yaml` にテンプレを列挙
  - UIで選択 → そのまま表示
- これで8〜12件のテンプレはすぐ回せる。

### 8.2 推奨技術（例）

- ローカルMVP：
  - Node.js + React（Vite） / または Next.js
  - YAML読み込み（`js-yaml`）
- もっと軽量：
  - Python + Streamlit

---

## 9. リポジトリ構成案（例：React/Vite）

```
/ (repo)
  /apps
    /web
      /src
        /components
        /pages
        /lib
        /types
      index.html
      vite.config.ts
  /packages
    /core
      patterns.yaml
      schema.ts
      generator.ts   # テストケース生成など
  /docs
    requirements.md
    decisions.md
  README.md
```

---

## 10. 命名規則（支払条件コード案の作り方：ツール側で提案）

### 10.1 例

- `JP_EOM_EOM_M1`：月末締→翌月末（+1 month, fixed 31）
- `JP_D20_EOM_M1M2`：20日限は+1月末、末日限は+2月末

### 10.2 ルール（案）

- Prefix：`JP_`
- 締日：`EOM` or `D20` `D15` など
- 支払日：`EOM` or `D10` `D25` など
- 追加月：`M1` `M2`
- 注意がある固定日系は suffix `FD` を付ける等

---

## 11. 実装タスク（チェックリスト）

- [ ] テンプレ定義ファイル `patterns.yaml` 作成（まず3件）
- [ ] テンプレ表示UI（Pattern選択→詳細表示）
- [ ] Baseline mode選択と warnings の条件出し分け
- [ ] OBB8 lines 表の表示
- [ ] テストケース（固定5本 or 生成）
- [ ] README（目的、使い方、免責、SAP固有設定の前提）

---

## 12. 免責・コンプライアンス（公開する場合の注意）

- SAP公式ドキュメントや画面のスクショ・表の転載は避け、
  - 「概念」「一般的な商習慣」「自作テンプレ」を中心にする。
- ツールは「設定値の確定」ではなく、
  - **検討支援（提案）** と **テスト観点** を主目的にする。

---

## 13. 次に決めること（要件確定）

- 初期テンプレの優先度（8〜12件）
- Baseline mode のデフォルト（推奨）
- 割引（スキント）をMVPで扱うか（なし→後で追加でもOK）

